/**
 * Created by lucadario on 23/04/17.
 */

/**
 * Delete a item in database with a id == itemId in a Bringit list with a id == listId
 */
Meteor.publish('deleteItem', function (listId,itemId) {
   //TODO chiamare manageListUC per eliminare un item
});
